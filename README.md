# Server

First practice with Golang. Simple server and client.

To install packages:
```bash
cd internal/server
go install
cd ../../internal/client
go install
```

To run example:
```bash
cd examples
go build -o server
./server
```

To run tests:
```bash
cd tests
go test -v
```
